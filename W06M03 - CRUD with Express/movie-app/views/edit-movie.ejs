<!DOCTYPE html>
<html lang="en">
    <head>
        <meta charset="UTF-8">
        <meta name="viewport" content="width=device-width, initial-scale=1.0">
        <title> Edit Movie</title>
    </head>
    <body>
        <h1> Edit any of the fields of the movie </h1>
        <form action="/api/edit/movie/<%= movie.id %>" method="POST">
            <div>
                <label for="title">
                    Title:
                </label>
                <input type="text" id="title" name="title" value="<%= movie.title %>">
            </div>
            <div>
                <label for="director">
                    Director:
                </label>
                <input type="text" id="director" name="director" value="<%= movie.director %>">
            </div>
            <button type="submit">
                Edit movie
            </button>
        </form>
    </body>
</html>